import styled from 'styled-components';

const Cards = styled.div`
    display: flex;
    flex-wrap: wrap;
    margin: 10px auto;
    padding: 10px;
`

const Styled = {
    Cards
}

export default Styled