import styled from 'styled-components';

const CardsContainer = styled.div`
  display: flex;
  flex-wrap: wrap;
  margin: 10px auto;
  padding: 10px;
`;

const Styled = {
  CardsContainer,
};

export default Styled;
